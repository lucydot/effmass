[build-system]
requires = [
    "setuptools >= 65",
    "setuptools_scm[toml]",
    "setuptools_scm_git_archive",
    "wheel"
]
build-backend = "setuptools.build_meta"

[tool.setuptools_scm]
write_to = "effmass/_version.py"
local_scheme = "no-local-version"